rootProject.name = 'adyen-java-spring-online-payments-authorisation-adjustment'

dependencyResolutionManagement {
    versionCatalogs {
        libs {
            from(files("../gradle/libs.versions.toml"))
        }
    }
}
